[project]
name = "perry-picks"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "joblib",
    "pyarrow",
    "nba_api",
    "requests",
    "xgboost",
    "lightgbm",
    "scikit-optimize",
]
